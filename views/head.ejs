<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="css/bulma.css">
        <link rel="stylesheet" href="css/styles.css">
        <title>Canvas Garden</title>
        <% if(locals.scripts) { %>
            <% for(let i = 0; i < scripts.length; i++) { %>
                <script src="<%= scripts[i] %>"></script>
            <% } %>
        <% } %>
    </head>
    <% if(locals.onload){ %>
        <body onload="<%= onload %>">
    <% } else{ %>
        <body>
    <% } %>